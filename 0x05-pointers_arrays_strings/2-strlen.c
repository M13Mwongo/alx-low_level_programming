#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: string to be checked
 *
 * Return: string length
 */
void _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}

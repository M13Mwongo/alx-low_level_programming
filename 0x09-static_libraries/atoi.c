#include "main.h"
#include <string.h>
/**
 * _puts - prints out a string followed by a new line to stdout
 * @str: string to be outputted
 *
 * Return: void
 */
int _atoi(char *s)
{
	return (strlen(s));
}

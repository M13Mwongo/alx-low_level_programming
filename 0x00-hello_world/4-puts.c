#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Declare variables*/
	char statement[] = "\"Programming is like building a multilingual puzzle";
	/*statements*/
	puts(statement);

	return (0);
}
